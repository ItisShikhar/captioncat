/**
 * Build-time choice for local registry faces.
 * Keep false so Studio loads Google/CDN sources on demand. Set true only when
 * the generated HTML must work offline with every bundled local source.
 */
export const BUNDLE_FONTS_WITH_REMOTE_SOURCES = false;
